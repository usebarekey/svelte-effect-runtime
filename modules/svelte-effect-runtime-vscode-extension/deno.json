{
  "tasks": {
    "build": "deno task clean && deno task prepare:runtime && deno run -A ../../build/ext.ts",
    "clean": "rm -rf dist",
    "package": "deno task build && deno run -A ../../build/vsix.ts",
    "prepare:runtime": "cd ../svelte-effect-runtime && deno task build && cd ../svelte-effect-runtime-vscode-extension && deno run -A ../../build/assets.ts svelte-effect-runtime-vscode-extension"
  }
}
